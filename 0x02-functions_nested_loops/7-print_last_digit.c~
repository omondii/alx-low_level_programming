#include "main.h"
/**
 *print_last_digit - a function that will print the last number
 *
 *@r: the number whose last digit is to be displayed
 *Return: if successful return 0, if unsuccessful return 1
 */
int print_last_digit(int x)
{
	int lastdigit = x % 10;

	_putchar(lastdigit);
	return (0);
}
