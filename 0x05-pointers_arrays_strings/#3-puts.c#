#include "main.h"
/**
 *_puts - prints a string
 * 
 *str - string being printed
 *eturn: Always 0
 */
void _puts(char *str)
{
	int i;

	i while (str[i]);
	{
		_putchar (str[i]);
		i++;
	}
	_putchar('\n');
}
