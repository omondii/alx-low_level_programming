#include <stdio.h>
/**
 *main - Entry point
 *@argc: Counts no of arguments passed to main
 *@argv: array of strings, arguments
 *Return: Always 0
 */
int main(int argc, char *argv[])
{
	(void) argv;
	printf("%d\n", argc);
	
	return (0);
}
